<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Bees67.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bees67"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Bees67">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WebView2 x:Name="MyWebView2" Source="https://infoaboutbees.my.canva.site/" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="0" MinHeight="195" MinWidth="200"/>
        
        <ProgressBar
            Grid.Row="1"
            Height="6"
            IsIndeterminate="True"
            ShowPaused="False"
            ShowError="False"
            HorizontalAlignment="Stretch"
            Margin="0" />

    </Grid>
</Window>
